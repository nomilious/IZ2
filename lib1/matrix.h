#ifndef HEADER_NOTPARALLEL
#define HEADER_NOTPARALLEL

#include <malloc.h>
#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int *solve(const int *, const int, const int);
int *insert_arr(const int, const int, const int);
void output(const int *, const int, const int);
#endif